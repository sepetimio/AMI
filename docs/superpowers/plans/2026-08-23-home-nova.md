# A home nova — plano de implementação

> **Para quem executa:** SUB-SKILL OBRIGATÓRIA: use `superpowers:subagent-driven-development` (recomendado) ou `superpowers:executing-plans`. Os passos usam caixinha (`- [ ]`).

**Objetivo:** a home deixa de ser a busca de médicos e passa a ser a porta da associação — uma faixa curta de identidade, um carrossel de banners que a AMI atualiza sozinha, e três cartões de serviço, com "Encontre um médico" virando um deles.

**Arquitetura:** o banner é um tipo novo no Sanity, ao lado de notícia e página institucional. O carrossel é construído sem biblioteca — rolagem com encaixe lateral mais um componente de cliente pequeno. As três seções novas entram acima das quatro que já existem, e o herói de tela cheia sai.

**Tecnologias:** Next.js 16.3.1 (App Router), React 19, Sanity (`next-sanity`, `@sanity/image-url`), Vitest, Tailwind 4.

**Spec:** `docs/superpowers/specs/2026-08-23-home-nova-decisoes.md`

## Restrições globais

- **Português em tudo que o usuário lê**
- **Descrição de imagem é obrigatória** em todo banner, e ela precisa dizer **o que o cartaz diz** — toda a informação do banner mora dentro da arte, e imagem é opaca para leitor de tela, para busca e para quem aumenta a fonte
- **Uma arte por banner, 3000 × 856** — o padrão do site de referência, escolhido pelo dono
- **Quem pede menos movimento no sistema não recebe rotação nenhuma.** O site já respeita `prefers-reduced-motion` em dois lugares; é seguir o que existe
- **Sem banner, a seção não existe.** Nada de espaço vazio nem "em breve"
- **Nenhuma biblioteca nova.** O projeto não tem nenhuma para carrossel, e um carrossel é encaixe de rolagem mais umas quarenta linhas
- **Nenhuma migração de banco.** Esta fatia não encosta no Supabase
- **Nenhum número de contraste escrito de memória.** Todo número em comentário nomeia os dois lados do par — essa confusão gerou cinco defeitos na fatia anterior
- Mensagens de commit **sem acento**
- Rodar: `npx vitest run` · Tipos: `npx tsc --noEmit` · Build: `npm run build`
- **Nunca use `Write` num arquivo que já existe sem ler antes**
- O repositório mistura CRLF e LF. Edições cirúrgicas

---

## A home, antes e depois

**Medido em `app/(site)/page.tsx`**, e corrige o documento de decisões, que dizia que a seção de notícias era nova — ela já existe, na linha 278.

| | hoje | depois |
|---|---|---|
| 1 | Herói de tela cheia, `<h1>Encontre um médico em Imperatriz</h1>` (linha 72) | **Faixa da AMI** — curta, com os números vivos |
| 2 | Especialidades (206) | **Carrossel de banners** |
| 3 | Institucional (229) | **Serviços da AMI** — três cartões |
| 4 | Últimas notícias (278) | Especialidades *(sem mudança)* |
| 5 | Onde os médicos atendem (285) | Institucional *(sem mudança)* |
| 6 | — | Últimas notícias *(sem mudança)* |
| 7 | — | Onde os médicos atendem *(sem mudança)* |

## Mapa de arquivos

**Criados**

| arquivo | responsabilidade |
|---|---|
| `sanity/schemas/banner.ts` | o tipo do banner no Studio |
| `lib/sanity/banners.ts` | a consulta e o tipo de domínio |
| `components/home/Carrossel.tsx` | o carrossel, componente de cliente |
| `components/home/FaixaDaAmi.tsx` | a faixa de identidade |
| `components/home/ServicosDaAmi.tsx` | os três cartões |
| `app/(site)/contato/page.tsx` | "Fale com a AMI" |
| `testes/banners.test.ts` | as regras do banner |
| `testes/home.test.ts` | a estrutura da home |

**Modificados**

`sanity/schemas/index.ts` · `lib/sanity/tipos.ts` · `app/(site)/page.tsx` · `lib/sanity/paginas.ts` · `lib/rascunhosLegais.ts` · `testes/paleta.test.ts` · `components/layout/MenuPrincipal.tsx`

---

## Tarefa 1: A dívida do teste de paleta

A fatia anterior terminou com esta dívida nomeada, e ela vem primeiro porque é independente de tudo o mais.

`testes/paleta.test.ts` tem **duas listas escritas à mão**: os tokens de texto, e os fundos. A de texto foi esquecida quatro vezes; a de fundos, nunca foi auditada até a revisão final achar dois buracos.

E existe um risco maior: **não há rede nenhuma contra classe morta.** Um token que sai do `@theme` e sobra num componente não gera CSS, não dá erro, e o elemento fica sem cor. Um revisor provou mutando, e o repositório já teve uma vítima — a `.grao`, uma `<div>` em toda página referenciando um estilo inexistente.

**Arquivos:**
- Modificar: `testes/paleta.test.ts`

**Interfaces:**
- Consome: `fonte(relativo)` de `testes/apoio.ts`
- Produz: `paresUsados(): { tinta: string; fundo: string }[]` — exportada, para uma fatia futura poder reusar

- [ ] **Passo 1: Escrever o teste que deriva as listas**

Acrescente a `testes/paleta.test.ts`:

```ts
import { readdirSync, readFileSync, statSync } from "node:fs";
import { fileURLToPath } from "node:url";

/** Todo arquivo .tsx sob app/ e components/, recursivo. */
function telas(relativo: string): string[] {
  const base = fileURLToPath(new URL(relativo, import.meta.url));
  const achados: string[] = [];
  for (const entrada of readdirSync(base, { withFileTypes: true })) {
    const caminho = `${base}/${entrada.name}`;
    if (entrada.isDirectory()) achados.push(...telas(`${relativo}/${entrada.name}`));
    else if (entrada.name.endsWith(".tsx")) achados.push(caminho);
  }
  return achados;
}

const FONTES = [...telas("../app"), ...telas("../components")]
  .map((c) => readFileSync(c, "utf8"))
  .join("\n");

/*
  Os tokens que o código realmente usa, achados no código.

  A versão anterior deste arquivo trazia duas listas escritas à mão. A de
  texto foi esquecida quatro vezes; a de fundos nunca foi auditada, e quando
  foi, tinha dois buracos — um deles o par mais apertado do sistema inteiro.

  Lista escrita à mão é uma foto do que alguém lembrou. Esta varre o código.
*/
function tokensEm(prefixo: string): string[] {
  const achados = new Set<string>();
  for (const m of FONTES.matchAll(new RegExp(`\\b(?:hover:)?${prefixo}-([a-z0-9-]+)\\b`, "g"))) {
    achados.add(m[1]);
  }
  return [...achados].filter((n) => T[n]).sort();
}

describe("as listas saem do código, não da memória", () => {
  it("acha token de texto e de fundo em uso", () => {
    /*
      Se a varredura devolver vazio, ela quebrou — e um teste que não mede
      nada passa em silêncio. Estes pisos existem para isso, e são folgados
      de propósito: o número exato muda a cada fatia.
    */
    expect(tokensEm("text").length).toBeGreaterThanOrEqual(5);
    expect(tokensEm("bg").length).toBeGreaterThanOrEqual(4);
  });

  it("todo token usado em text- ou bg- está declarado no @theme", () => {
    /*
      Esta é a rede contra classe morta. Um token que sai do @theme e sobra
      num componente não gera CSS, não dá erro, e o elemento fica sem cor —
      um revisor provou mutando, e o repositório já teve uma vítima.
    */
    const orfaos: string[] = [];
    for (const prefixo of ["text", "bg", "border"]) {
      for (const m of FONTES.matchAll(
        new RegExp(`\\b(?:hover:)?${prefixo}-(ami-[a-z0-9-]+|ink-[0-9]+|canvas|surface[a-z-]*|line[a-z-]*|warn|danger)\\b`, "g"),
      )) {
        if (!T[m[1]]) orfaos.push(`${prefixo}-${m[1]}`);
      }
    }
    expect([...new Set(orfaos)], "classe que aponta para token que não existe").toEqual([]);
  });
});
```

- [ ] **Passo 2: Rodar**

```bash
npx vitest run testes/paleta.test.ts
```

Esperado: PASSA. Se a segunda asserção falhar, ela achou classe morta de verdade — **anote quais e me avise antes de consertar**, porque pode ser defeito anterior a esta fatia.

- [ ] **Passo 3: Trocar as duas listas à mão pela varredura**

Onde hoje há `const TEXTO_DE_CORPO = [...]` escrito à mão, use `tokensEm("text")`. Onde há `for (const fundo of ["canvas", "surface", ...])`, use `tokensEm("bg")`.

**Cuidado:** `ami-lima-400` é texto **só sobre fundo escuro**, e medi-lo contra fundo claro daria o par errado. Mantenha a exceção que já existe no arquivo, e escreva no comentário que ela é a única.

- [ ] **Passo 4: Provar que a rede nova morde**

Acrescente `className="bg-ami-mint-100"` — token apagado na fatia anterior — a qualquer componente.

```bash
npx vitest run testes/paleta.test.ts
```

Esperado: **FALHA** em "classe que aponta para token que não existe". Antes desta tarefa, `tsc`, os testes e o build passavam os três. Desfaça e cole as duas saídas.

- [ ] **Passo 5: Conferir e commitar**

```bash
npx vitest run && npx tsc --noEmit
git add testes/paleta.test.ts
git commit -m "As listas do teste de paleta saem do codigo, nao da memoria

Duas listas escritas a mao: a de texto foi esquecida quatro vezes, a de
fundos nunca foi auditada. E nao havia rede nenhuma contra classe morta —
token que sai do @theme e sobra num componente nao gera CSS e nao da erro."
```

---

## Tarefa 2: O banner no Sanity

**Arquivos:**
- Criar: `sanity/schemas/banner.ts`, `lib/sanity/banners.ts`, `testes/banners.test.ts`
- Modificar: `sanity/schemas/index.ts`, `lib/sanity/tipos.ts`

**Interfaces:**
- Consome: o padrão de `sanity/schemas/noticia.ts` (imagem com `alt` obrigatório)
- Produz:
  - `type Banner = { id: string; nome: string; imagem: string; alt: string; destino: string | null; ordem: number }`
  - `bannersAtivos(): Promise<Banner[]>`
  - `estaNoAr(b: { expiraEm: string | null }, agora: Date): boolean` — pura e exportada

- [ ] **Passo 1: Ler o padrão antes de escrever**

```bash
sed -n '1,70p' sanity/schemas/noticia.ts
sed -n '1,30p' lib/sanity/consultas.ts
```

O `alt` obrigatório dentro do campo de imagem é o padrão do projeto. Siga.

- [ ] **Passo 2: Escrever o teste da regra de validade**

Crie `testes/banners.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { estaNoAr } from "@/lib/sanity/banners";

const AGORA = new Date("2026-08-23T12:00:00Z");

describe("estaNoAr", () => {
  it("sem data de validade, fica para sempre", () => {
    expect(estaNoAr({ expiraEm: null }, AGORA)).toBe(true);
  });

  it("com data no futuro, aparece", () => {
    expect(estaNoAr({ expiraEm: "2026-09-01" }, AGORA)).toBe(true);
  });

  it("com data de ontem, some", () => {
    expect(estaNoAr({ expiraEm: "2026-08-22" }, AGORA)).toBe(false);
  });

  it("no proprio dia da validade, ainda aparece", () => {
    /*
      "Aparece até 23/08" significa que 23/08 é o último dia em que aparece,
      não o primeiro em que some. Quem preenche pensa na data do evento.
    */
    expect(estaNoAr({ expiraEm: "2026-08-23" }, AGORA)).toBe(true);
  });
});
```

- [ ] **Passo 3: Rodar e ver falhar**

```bash
npx vitest run testes/banners.test.ts
```

Esperado: FALHA — o módulo não existe.

- [ ] **Passo 4: Escrever o esquema**

Crie `sanity/schemas/banner.ts`:

```ts
import { defineField, defineType } from "sanity";

/*
  O banner da home.

  É uma ARTE PRONTA, produzida fora nas dimensões padrão — não é texto sobre
  foto. A AMI sobe a imagem e ela aparece.

  Por isso a descrição da imagem não é cortesia: toda a informação do banner
  mora dentro da arte, e imagem é opaca para leitor de tela, para busca e para
  quem aumenta a fonte. A descrição é o único caminho do conteúdo até essas
  pessoas.
*/
export const banner = defineType({
  name: "banner",
  title: "Banner da home",
  type: "document",
  fields: [
    defineField({
      name: "nome",
      title: "Nome interno",
      type: "string",
      description: "Só para você achar este banner na lista. Não aparece no site.",
      validation: (r) => r.required().max(80),
    }),
    defineField({
      name: "imagem",
      title: "Arte",
      type: "image",
      description: "3000 × 856 pixels. Texto grande: a arte encolhe no celular.",
      validation: (r) => r.required(),
      fields: [
        defineField({
          name: "alt",
          title: "Descrição da imagem",
          type: "string",
          description:
            "Diga o que o cartaz diz, não o que ele é. " +
            'Não "banner da assembleia", e sim "Assembleia geral no dia 12 de março, ' +
            'às 19h, na sede da AMI".',
          validation: (r) => r.required().min(15),
        }),
      ],
    }),
    defineField({
      name: "destino",
      title: "Para onde leva",
      type: "string",
      description:
        "Endereço de dentro do site, começando com barra — /medicos, /noticias. " +
        "Deixe vazio se o banner só informa.",
      validation: (r) =>
        r.custom((v) =>
          !v || (typeof v === "string" && v.startsWith("/"))
            ? true
            : "O endereço precisa começar com barra.",
        ),
    }),
    defineField({
      name: "ordem",
      title: "Ordem",
      type: "number",
      description: "10, 20, 30 — com folga, para inserir no meio sem renumerar tudo.",
      initialValue: 10,
      validation: (r) => r.required(),
    }),
    defineField({
      name: "expiraEm",
      title: "Aparece até",
      type: "date",
      description:
        "O último dia em que ele aparece. Vazio significa que fica para sempre. " +
        "Banner de assembleia ou de prazo some sozinho no dia seguinte.",
    }),
  ],
  preview: {
    select: { title: "nome", subtitle: "expiraEm", media: "imagem" },
  },
});
```

- [ ] **Passo 5: Registrar no índice**

Em `sanity/schemas/index.ts`, importe `banner` e acrescente ao vetor `tipos`.

- [ ] **Passo 6: Escrever a consulta**

Crie `lib/sanity/banners.ts`:

```ts
import imageUrlBuilder from "@sanity/image-url";
import { defineQuery } from "next-sanity";
import { clienteSanity } from "@/lib/sanity/cliente";

export type Banner = {
  id: string;
  nome: string;
  imagem: string;
  alt: string;
  destino: string | null;
  ordem: number;
};

/*
  Banner vencido some sozinho, mas continua no Sanity: a AMI reaproveita no
  ano seguinte trocando a data.

  A comparação é feita AQUI e não no GROQ porque o site é gerado
  estaticamente e revalida de hora em hora — uma data resolvida no servidor
  do Sanity seria a data da geração, não a de quem visita.
*/
export function estaNoAr(b: { expiraEm: string | null }, agora: Date): boolean {
  if (!b.expiraEm) return true;
  const fim = new Date(`${b.expiraEm}T23:59:59Z`);
  return fim.getTime() >= agora.getTime();
}

export const GROQ_BANNERS = defineQuery(`
  *[_type == "banner" && defined(imagem.asset)] | order(ordem asc) {
    "id": _id,
    nome,
    imagem,
    "alt": imagem.alt,
    destino,
    ordem,
    "expiraEm": expiraEm
  }
`);

export async function bannersAtivos(): Promise<Banner[]> {
  const cru = await clienteSanity.fetch(GROQ_BANNERS, {}, {
    next: { tags: ["banners"] },
  });

  const construtor = imageUrlBuilder(clienteSanity);
  const agora = new Date();

  /* eslint-disable-next-line @typescript-eslint/no-explicit-any */
  return ((cru ?? []) as any[])
    .filter((b) => estaNoAr(b, agora))
    .map((b) => ({
      id: b.id,
      nome: b.nome,
      imagem: construtor.image(b.imagem).width(3000).height(856).fit("crop").url(),
      alt: b.alt ?? "",
      destino: b.destino ?? null,
      ordem: b.ordem ?? 0,
    }));
}
```

**Leia `lib/sanity/cliente.ts` antes** — se o cliente tiver outro nome, o arquivo manda.

- [ ] **Passo 7: Rodar e ver passar**

```bash
npx vitest run testes/banners.test.ts
npx tsc --noEmit
```

- [ ] **Passo 8: Provar que a regra morde**

Troque `fim.getTime() >= agora.getTime()` por `>`. O caso "no próprio dia da validade" tem que ficar vermelho. Desfaça e cole as duas saídas.

- [ ] **Passo 9: Commit**

```bash
git add -A
git commit -m "O banner da home vira um tipo no Sanity

E uma arte pronta, nao texto sobre foto. A descricao da imagem e obrigatoria
porque toda a informacao mora dentro da arte, e imagem e opaca para leitor de
tela, para busca e para quem aumenta a fonte.

O campo que mais importa e 'aparece ate': banner de assembleia morre numa
data, e sem ele o site anuncia em julho uma reuniao de marco."
```

---

## Tarefa 3: O carrossel

**Arquivos:**
- Criar: `components/home/Carrossel.tsx`

**Interfaces:**
- Consome: da tarefa 2, `type Banner`
- Produz: `<Carrossel banners={Banner[]} />`

- [ ] **Passo 1: Escrever o componente**

Crie `components/home/Carrossel.tsx`:

```tsx
"use client";

import Image from "next/image";
import Link from "next/link";
import { useEffect, useRef, useState } from "react";
import type { Banner } from "@/lib/sanity/banners";

const INTERVALO = 6000;

/*
  O carrossel de banners, sem biblioteca.

  Encaixe de rolagem faz o trabalho pesado: arrastar no celular vem de graça,
  e a posição é o próprio `scrollLeft`. O JavaScript só empurra.

  A rotação para em quatro situações: alguém pausa, o mouse entra, o teclado
  chega, ou a aba sai da frente — não faz sentido girar para ninguém.

  Quem liga "reduzir movimento" no sistema NÃO recebe rotação nenhuma. Quem
  liga isso costuma ter enxaqueca, vertigem ou epilepsia fotossensível: para
  essas pessoas, coisa que se move sozinha não é incômodo, é sintoma. Nesse
  caso o botão de pausa some, porque não há o que pausar.
*/
export function Carrossel({ banners }: { banners: Banner[] }) {
  const trilho = useRef<HTMLDivElement>(null);
  const [atual, setAtual] = useState(0);
  const [pausado, setPausado] = useState(false);
  const [semMovimento, setSemMovimento] = useState(false);

  useEffect(() => {
    const consulta = window.matchMedia("(prefers-reduced-motion: reduce)");
    const aplicar = () => setSemMovimento(consulta.matches);
    aplicar();
    consulta.addEventListener("change", aplicar);
    return () => consulta.removeEventListener("change", aplicar);
  }, []);

  const gira = banners.length > 1 && !semMovimento && !pausado;

  useEffect(() => {
    if (!gira) return;
    const t = setInterval(() => irPara((atual + 1) % banners.length), INTERVALO);
    return () => clearInterval(t);
  });

  function irPara(i: number) {
    const el = trilho.current;
    if (!el) return;
    el.scrollTo({ left: el.clientWidth * i, behavior: semMovimento ? "auto" : "smooth" });
    setAtual(i);
  }

  if (banners.length === 0) return null;

  const varios = banners.length > 1;

  return (
    <section
      aria-label="Destaques da AMI"
      className="mx-auto max-w-[1200px] px-4 md:px-6"
      onMouseEnter={() => setPausado(true)}
      onMouseLeave={() => setPausado(false)}
      onFocusCapture={() => setPausado(true)}
      onBlurCapture={() => setPausado(false)}
    >
      <div className="relative">
        <div
          ref={trilho}
          onScroll={(e) => {
            const el = e.currentTarget;
            setAtual(Math.round(el.scrollLeft / el.clientWidth));
          }}
          className="flex snap-x snap-mandatory overflow-x-auto rounded-bloco [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"
        >
          {banners.map((b) => {
            const arte = (
              <Image
                src={b.imagem}
                alt={b.alt}
                width={3000}
                height={856}
                priority={b.id === banners[0].id}
                className="h-auto w-full"
              />
            );
            return (
              <div key={b.id} className="w-full shrink-0 snap-start">
                {b.destino ? <Link href={b.destino}>{arte}</Link> : arte}
              </div>
            );
          })}
        </div>
      </div>

      {varios ? (
        <div className="mt-4 flex items-center justify-center gap-3">
          <button
            type="button"
            onClick={() => irPara((atual - 1 + banners.length) % banners.length)}
            className="pressiona rounded-controle border border-line px-3 py-2 text-[14px] text-ink-600 hover:text-ink-900"
          >
            Anterior
          </button>

          <div className="flex gap-2">
            {banners.map((b, i) => (
              <button
                key={b.id}
                type="button"
                onClick={() => irPara(i)}
                aria-label={`Ir para o banner ${i + 1} de ${banners.length}`}
                aria-current={i === atual}
                className={
                  i === atual
                    ? "size-2.5 rounded-full bg-ami-green-600"
                    : "size-2.5 rounded-full bg-line-strong"
                }
              />
            ))}
          </div>

          <button
            type="button"
            onClick={() => irPara((atual + 1) % banners.length)}
            className="pressiona rounded-controle border border-line px-3 py-2 text-[14px] text-ink-600 hover:text-ink-900"
          >
            Próximo
          </button>

          {semMovimento ? null : (
            <button
              type="button"
              onClick={() => setPausado((p) => !p)}
              className="pressiona ml-2 rounded-controle border border-line px-3 py-2 text-[14px] font-medium text-ink-600 hover:text-ink-900"
            >
              {pausado ? "Retomar" : "Pausar"}
            </button>
          )}
        </div>
      ) : null}
    </section>
  );
}
```

- [ ] **Passo 2: Conferir tipos e build**

```bash
npx tsc --noEmit
npm run build
```

- [ ] **Passo 3: Commit**

```bash
git add -A
git commit -m "O carrossel de banners, sem biblioteca

Encaixe de rolagem faz o trabalho: arrastar no celular vem de graca. Quem
liga 'reduzir movimento' no sistema nao recebe rotacao nenhuma, e o botao de
pausa some junto — nao ha o que pausar."
```

---

## Tarefa 4: A faixa da AMI e os três cartões

**Arquivos:**
- Criar: `components/home/FaixaDaAmi.tsx`, `components/home/ServicosDaAmi.tsx`

**Interfaces:**
- Consome: `AMI` de `lib/ami.ts`, `telefoneParaLigar`, `formatarTelefone`
- Produz: `<FaixaDaAmi total={number} especialidades={number} bairros={number} />` e `<ServicosDaAmi total={number} especialidades={number} ultimaNoticia={{ titulo: string; slug: string } | null} />`

- [ ] **Passo 1: Ler o que já existe**

```bash
sed -n '1,60p' lib/ami.ts
sed -n '110,150p' "app/(site)/page.tsx"
```

Os números vivos (total, especialidades, bairros) já são calculados na home e passados ao herói. A faixa recebe os mesmos.

- [ ] **Passo 2: Escrever a faixa**

Crie `components/home/FaixaDaAmi.tsx`:

```tsx
import { AMI } from "@/lib/ami";

/*
  A identidade, curta.

  Substitui o herói de tela cheia, cujo título era "Encontre um médico em
  Imperatriz" — o site inteiro era a busca. Agora a busca é um dos serviços,
  e o topo diz o que a AMI é.

  Os números saem do banco e nunca são escritos à mão. São a coisa mais
  honesta da home: provam que ela é mantida.
*/
export function FaixaDaAmi({
  total,
  especialidades,
  bairros,
}: {
  total: number;
  especialidades: number;
  bairros: number;
}) {
  return (
    <section className="bg-ami-green-900 py-12 md:py-16">
      <div className="mx-auto max-w-[1200px] px-4 md:px-6">
        <h1 className="texto-placa text-surface">{AMI.razaoSocial}</h1>

        <p className="mt-5 max-w-[52ch] text-[18px] leading-relaxed text-ami-lima-400 md:text-[20px]">
          A associação dos médicos de Imperatriz. Aqui você encontra quem atende
          na cidade, fala com a associação e se torna associado.
        </p>

        <p className="registro mt-6 text-[15px] text-surface/80">
          <strong className="font-semibold text-surface">{total}</strong>{" "}
          {total === 1 ? "profissional" : "profissionais"} ·{" "}
          <strong className="font-semibold text-surface">{especialidades}</strong>{" "}
          {especialidades === 1 ? "especialidade" : "especialidades"} ·{" "}
          <strong className="font-semibold text-surface">{bairros}</strong>{" "}
          {bairros === 1 ? "bairro" : "bairros"}
        </p>
      </div>
    </section>
  );
}
```

**Medido:** `lib/ami.ts` não tem campo `nome`. Tem `razaoSocial` ("Associação Médica de
Imperatriz") e `sigla` ("AMI"). Use `razaoSocial`.

- [ ] **Passo 3: Escrever os três cartões**

Crie `components/home/ServicosDaAmi.tsx`:

```tsx
import Link from "next/link";
import { AMI, formatarTelefone, telefoneParaLigar } from "@/lib/ami";

/*
  Os três serviços da AMI.

  Cada cartão mostra algo VIVO que o menu do topo não mostra — senão ele seria
  um segundo menu. O primeiro traz a contagem, o segundo o telefone clicável,
  o terceiro a manchete mais recente.

  A grade nasce com três e já comporta "Sua AMI" (aluguel de auditório e hall
  de eventos) e "Empresa parceira", que o dono anunciou para depois.
*/
export function ServicosDaAmi({
  total,
  especialidades,
  ultimaNoticia,
}: {
  total: number;
  especialidades: number;
  ultimaNoticia: { titulo: string; slug: string } | null;
}) {
  const fixo = AMI.telefones[0];

  return (
    <section className="mx-auto max-w-[1200px] px-4 py-16 md:px-6 md:py-20">
      <h2 className="font-titulo text-[15px] font-bold uppercase tracking-[0.1em] text-ink-400">
        Serviços da AMI
      </h2>

      <div className="mt-6 grid gap-4 md:grid-cols-3">
        <Link
          href="/medicos"
          className="pressiona rounded-bloco border border-line bg-surface p-6 hover:border-ami-green-600 hover:shadow-erguido"
        >
          <h3 className="text-[21px] font-semibold text-ink-900">Encontre um médico</h3>
          <p className="registro mt-3 text-[15px] text-ink-400">
            {total} {total === 1 ? "profissional" : "profissionais"} em{" "}
            {especialidades} {especialidades === 1 ? "especialidade" : "especialidades"}
          </p>
        </Link>

        <Link
          href="/contato"
          className="pressiona rounded-bloco border border-line bg-surface p-6 hover:border-ami-green-600 hover:shadow-erguido"
        >
          <h3 className="text-[21px] font-semibold text-ink-900">Fale com a AMI</h3>
          <p className="registro mt-3 text-[15px] text-ink-400">
            {formatarTelefone(fixo)}
          </p>
        </Link>

        <Link
          href="/associacao/seja-associado"
          className="pressiona rounded-bloco border border-line bg-surface p-6 hover:border-ami-green-600 hover:shadow-erguido"
        >
          <h3 className="text-[21px] font-semibold text-ink-900">Seja associado</h3>
          <p className="mt-3 text-[15px] text-ink-400">
            Médico com inscrição no CRM pode se associar à AMI.
          </p>
        </Link>
      </div>

      {ultimaNoticia ? (
        <Link
          href={`/noticias/${ultimaNoticia.slug}`}
          className="pressiona mt-4 block rounded-bloco border border-line bg-surface p-6 hover:border-ami-green-600 hover:shadow-erguido"
        >
          <h3 className="font-titulo text-[13px] font-bold uppercase tracking-[0.1em] text-ink-400">
            Última notícia
          </h3>
          <p className="mt-2 text-[19px] font-semibold text-ink-900">
            {ultimaNoticia.titulo}
          </p>
        </Link>
      ) : null}
    </section>
  );
}
```

`telefoneParaLigar` fica sem uso aqui — **tire o import** se não usar, para não deixar código morto.

- [ ] **Passo 4: Conferir**

```bash
npx tsc --noEmit && npm run build
```

- [ ] **Passo 5: Commit**

```bash
git add -A
git commit -m "A faixa da AMI e os tres cartoes de servico

O topo deixa de dizer 'Encontre um medico em Imperatriz' e passa a dizer o
que a AMI e. A busca vira um dos cartoes.

Cada cartao mostra algo vivo que o menu nao mostra — contagem, telefone,
manchete — senao ele seria um segundo menu."
```

---

## Tarefa 5: As duas páginas novas

**Arquivos:**
- Criar: `app/(site)/contato/page.tsx`
- Modificar: `lib/sanity/paginas.ts`, `lib/rascunhosLegais.ts`, `app/(site)/associacao/[pagina]/page.tsx`, `components/layout/MenuPrincipal.tsx`

**Interfaces:**
- Consome: `AMI` de `lib/ami.ts`, o padrão de `components/editorial/RascunhoLegalNaTela.tsx`

- [ ] **Passo 1: Ler os três padrões**

```bash
sed -n '1,40p' components/editorial/RascunhoLegalNaTela.tsx
sed -n '1,40p' lib/rascunhosLegais.ts
cat lib/sanity/paginas.ts
```

- [ ] **Passo 2: Escrever "Fale com a AMI"**

Crie `app/(site)/contato/page.tsx`, com `export const revalidate = 3600` e `generateMetadata`. Mostre, tirando tudo de `lib/ami.ts`: o nome, o endereço em linha, os dois telefones como links de `tel:`, o Instagram, e o CNPJ.

Acrescente uma frase, e ela é a única coisa que não vem do arquivo:

> "Médico interessado em se associar: ligue para a AMI. A página de filiação está em preparação."

- [ ] **Passo 3: Acrescentar `seja-associado` à lista fechada**

Em `lib/sanity/paginas.ts`, acrescente `seja-associado` a `CAMINHO_DAS_PAGINAS` com o rótulo "Seja associado".

- [ ] **Passo 4: Escrever o rascunho provisório**

Em `lib/rascunhosLegais.ts`, acrescente uma entrada para "Seja associado", seguindo a forma das que já existem.

**O texto não inventa fato.** Sem valor de anuidade, sem lista de benefícios, sem requisitos além do óbvio. O que pode ser dito com verdade:

- o que é a AMI, tirado de `lib/ami.ts`
- que a associação é de médicos com inscrição no conselho
- como falar com a AMI para se associar, com os telefones reais

- [ ] **Passo 5: Fazer a rota mostrar o rascunho em vez de 404**

Em `app/(site)/associacao/[pagina]/page.tsx`, quando não houver documento no Sanity **e** existir rascunho em código para aquele endereço, renderize o rascunho com o aviso de `RascunhoLegalNaTela`. Sem rascunho, continua `notFound()`.

- [ ] **Passo 6: Acrescentar "Contato" ao menu**

Em `components/layout/MenuPrincipal.tsx`, acrescente `{ rotulo: "Contato", curto: "Contato", href: "/contato" }`.

- [ ] **Passo 7: Conferir**

```bash
npx vitest run && npx tsc --noEmit && npm run build
```

As duas rotas novas têm que aparecer na contagem de páginas do build.

- [ ] **Passo 8: Commit**

```bash
git add -A
git commit -m "Fale com a AMI e Seja associado

O contato sai inteiro de lib/ami.ts, recebido do cliente em 21/08/2026.

Seja associado nasce com rascunho assinalado, pelo mesmo mecanismo das
paginas legais — o comentario de RascunhoLegalNaTela explica: sem ele, um
cartao da home levaria a 404 ate a AMI escrever. O provisorio nao inventa
fato: sem valor de anuidade, sem lista de beneficios."
```

---

## Tarefa 6: A home montada

**Arquivos:**
- Modificar: `app/(site)/page.tsx`
- Criar: `testes/home.test.ts`

**Interfaces:**
- Consome: tudo das tarefas 2 a 5

- [ ] **Passo 1: Escrever o teste da estrutura**

Crie `testes/home.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { fonte, semComentarios } from "@/testes/apoio";

const HOME = semComentarios(fonte("../app/(site)/page.tsx"));

describe("a home", () => {
  it("monta as tres secoes novas", () => {
    for (const c of ["FaixaDaAmi", "Carrossel", "ServicosDaAmi"]) {
      expect(HOME, `falta <${c}> na home`).toContain(`<${c}`);
    }
  });

  it("mantem as quatro secoes que ja existiam", () => {
    for (const c of ["IndiceEspecialidades", "UltimasNoticias", "LadrilhosBairros"]) {
      expect(HOME, `a home perdeu <${c}>`).toContain(`<${c}`);
    }
  });

  it("o titulo da pagina nao fala mais de buscar medico", () => {
    /*
      O <h1> era "Encontre um médico em Imperatriz" e ocupava a tela inteira.
      Ele desceu para o cartao de servico. Se voltar ao topo, o site voltou a
      ser a busca em vez da porta da associacao.
    */
    expect(HOME).not.toMatch(/<h1[^>]*>\s*Encontre um médico/);
  });

  it("a faixa vem antes do carrossel, e o carrossel antes dos servicos", () => {
    const faixa = HOME.indexOf("<FaixaDaAmi");
    const carrossel = HOME.indexOf("<Carrossel");
    const servicos = HOME.indexOf("<ServicosDaAmi");
    expect(faixa).toBeLessThan(carrossel);
    expect(carrossel).toBeLessThan(servicos);
  });
});
```

- [ ] **Passo 2: Rodar e ver falhar**

```bash
npx vitest run testes/home.test.ts
```

Esperado: FALHA nas três primeiras asserções.

- [ ] **Passo 3: Montar a home**

Em `app/(site)/page.tsx`:

- **apague a seção do herói inteira** — hoje começa na linha 72, com `bg-ami-green-900`, e vai até antes da seção de especialidades
- carregue os banners e a última notícia junto com o que já é carregado:

```tsx
  const [especialidades, bairros, total, banners, noticias] = await Promise.all([
    /* … o que já está aqui … */
    bannersAtivos(),
    listarNoticias(1),
  ]);
```

- monte, nesta ordem, antes das seções que já existem:

```tsx
      <FaixaDaAmi total={total} especialidades={especialidades.length} bairros={bairros.length} />
      <Carrossel banners={banners} />
      <ServicosDaAmi
        total={total}
        especialidades={especialidades.length}
        ultimaNoticia={
          noticias[0] ? { titulo: noticias[0].titulo, slug: noticias[0].slug } : null
        }
      />
```

**Medido:** `ResumoNoticia` tem `titulo` e `slug`, com essa grafia. Pode usar direto.

Remova os imports que ficarem sem uso.

- [ ] **Passo 4: Rodar e ver passar**

```bash
npx vitest run && npx tsc --noEmit && npm run build
```

- [ ] **Passo 5: Provar que o teste morde**

Tire `<Carrossel …>` da home. O teste tem que ficar vermelho em duas asserções. Desfaça.

- [ ] **Passo 6: Commit**

```bash
git add -A
git commit -m "A home vira a porta da associacao

O heroi de tela cheia sai. Entram a faixa da AMI, o carrossel de banners e os
tres cartoes de servico, acima das quatro secoes que ja existiam.

O titulo 'Encontre um medico em Imperatriz' deixa de ser o assunto da pagina
e vira um cartao — que e como o dono pediu que fosse comunicado."
```

---

## Tarefa 7: A conferência, e a promessa que eu fiz

**Arquivos:**
- Modificar: `docs/estado-do-projeto.md`

- [ ] **Passo 1: Rodar tudo**

```bash
npx vitest run && npx tsc --noEmit && npm run build
```

- [ ] **Passo 2: Subir a produção**

```bash
npx next start -p 3100
```

Com o `npm run dev` desligado.

- [ ] **Passo 3: A conferência que faltou na fatia anterior**

A fatia da paleta foi entregue e o dono não viu diferença nenhuma. Cinco revisões mediram se estava **correto**, e nenhuma mediu se estava **diferente**.

Baixe o HTML da home e confirme, sem depender de ver:

```bash
curl -s http://localhost:3100/ > /tmp/home-nova.html
grep -c "Encontre um médico" /tmp/home-nova.html
grep -o "<h1[^>]*>[^<]*" /tmp/home-nova.html
grep -c "Serviços da AMI" /tmp/home-nova.html
```

Esperado: o `<h1>` **não** é mais "Encontre um médico em Imperatriz", e "Serviços da AMI" aparece.

**Diga no relatório o que o `<h1>` passou a ser.** É a prova de que a mudança é visível — o título maior da página mudou de assunto.

- [ ] **Passo 4: Confirmar o estado vazio**

Não há banner nenhum no Sanity ainda. Confirme que a home **não** mostra moldura de carrossel vazia, nem setas, nem bolinhas — a seção simplesmente não existe.

- [ ] **Passo 5: Derrubar o servidor e atualizar o documento de estado**

Registre a home nova, o banner no Sanity, as duas páginas novas, e que a AMI precisa subir a primeira arte para o carrossel aparecer.

- [ ] **Passo 6: Commit**

```bash
git add -A
git commit -m "Registra a home nova no estado do projeto"
```

---

## Autorrevisão do plano

**Cobertura da spec:**

| decisão da spec | tarefa |
|---|---|
| A home vira sete seções | 6 |
| Faixa da AMI com números vivos | 4 |
| Banner é arte pronta, no Sanity | 2 |
| Uma arte, 3000 × 856 | 2 |
| "Aparece até" some sozinho | 2 |
| Descrição de imagem obrigatória | 2 |
| Carrossel sem biblioteca, gira com pausa | 3 |
| Menos movimento não recebe rotação | 3 |
| Um banner só: sem setas nem bolinhas | 3 |
| Sem banner: seção não existe | 3 e 7 |
| Serviços da AMI, três cartões vivos | 4 |
| Fale com a AMI | 5 |
| Seja associado com rascunho | 5 |
| A dívida do teste de paleta | 1 |

**Marcadores por preencher:** nenhum.

**Consistência de nomes:** `Banner`, `bannersAtivos`, `estaNoAr` nascem na tarefa 2 e são consumidos com essa grafia nas tarefas 3 e 6. `FaixaDaAmi`, `Carrossel`, `ServicosDaAmi` nascem nas 3 e 4 e são montados na 6 com os mesmos nomes.

**Três coisas que eu declaro em vez de esconder:**

1. **A seção de notícias já existia** na home, na linha 278. O documento de decisões dizia que era nova. Corrigido no mapa acima.
2. **Eu escrevi `AMI.nome` na primeira versão deste plano, e esse campo não existe.** Medi antes de commitar: são `razaoSocial` e `sigla`. Corrigido na tarefa 4. Cada tarefa que toca `lib/ami.ts` continua mandando ler o arquivo antes.
3. **O rascunho de "Seja associado" muda o comportamento de `/associacao/[pagina]`** para todas as páginas institucionais, não só essa: onde antes dava 404, agora dá rascunho **se houver rascunho em código**. Como só `seja-associado` terá um, o efeito prático é limitado — mas é uma mudança de comportamento numa rota compartilhada, e a revisão deve olhar.
